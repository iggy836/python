解説
isalnum() メソッドを使用して、文字列が英数字のみで構成されているかを判定します。
このメソッドは以下の場合にTrueを返します：
半角アルファベット（A-Z, a-z）
半角数字（0-9）
以下の場合はFalseを返します：
スペース
特殊文字（!@#$など）
全角文字（日本語など）
空文字列
input() 関数でユーザーからの入力を受け取り、is_alphanumeric() 関数で判定を行います。
このプログラムは文字列の検証によく使用され、ユーザー名やパスワードの入力チェックなどで活用できます。